export const Colors = {
  // Softer Dark Theme Colors
  primary: '#FF6B35', // Keeping the softer orange/red
  accent: '#4CAF50', // Keeping the green accent
  background: '#2C2C2C', // Lighter dark grey background
  surface: '#363636', // Slightly lighter than background for cards
  textPrimary: '#E0E0E0', // Slightly off-white for better readability
  textSecondary: '#A0A0A0', // Softer grey for secondary text
  border: '#404040', // Keeping the border color
  error: '#FF5252', // Keeping the softer red
};

// You can define a dark theme here as well, or use a function to switch them.
// For example:
/*
export const DarkColors = {
  primary: '#FF6347',
  accent: '#8BC34A',
  background: '#121212',
  surface: '#1E1E1E',
  textPrimary: '#FFFFFF',
  textSecondary: '#BBBBBB',
  border: '#333333',
  error: '#FF6666',
};

export const getThemeColors = (isDarkMode) => {
  return isDarkMode ? DarkColors : Colors;
};
*/ 